services:
  web:
    build: .
    ports:
      - "3200:3000"
    env_file: .env
    environment:
      - DB_PATH=/app/data/odd-fellow.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
