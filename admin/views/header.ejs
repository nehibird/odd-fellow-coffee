<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Odd Fellow Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <link href="<%= base %>/css/admin.css" rel="stylesheet">
</head>
<body>
  <div class="d-flex">
    <nav class="sidebar d-flex flex-column p-3">
      <div class="mb-4">
        <h5 class="text-white mb-0">Odd Fellow Coffee</h5>
        <small class="text-muted">Admin Panel</small>
      </div>
      <ul class="nav flex-column flex-grow-1">
        <li class="nav-item">
          <a class="nav-link <%= currentPath === '/' ? 'active' : '' %>" href="<%= base %>/">
            <i class="bi bi-grid-1x2 me-2"></i> Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= currentPath.startsWith('/orders') ? 'active' : '' %>" href="<%= base %>/orders">
            <i class="bi bi-bag me-2"></i> Orders
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= currentPath.startsWith('/products') ? 'active' : '' %>" href="<%= base %>/products">
            <i class="bi bi-box-seam me-2"></i> Products
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= currentPath.startsWith('/subscriptions') ? 'active' : '' %>" href="<%= base %>/subscriptions">
            <i class="bi bi-arrow-repeat me-2"></i> Subscriptions
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= currentPath.startsWith('/calendar') ? 'active' : '' %>" href="<%= base %>/calendar">
            <i class="bi bi-calendar3 me-2"></i> Calendar
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= currentPath.startsWith('/drops') ? 'active' : '' %>" href="<%= base %>/drops">
            <i class="bi bi-fire me-2"></i> Drops
          </a>
        </li>
      </ul>
      <hr class="border-secondary mt-auto">
      <a href="<%= base %>/logout" class="nav-link text-muted"><i class="bi bi-box-arrow-left me-2"></i> Sign Out</a>
    </nav>

    <main class="main-content flex-grow-1 p-4">
      <% if (flash && flash.message) { %>
        <div class="alert alert-<%= flash.type === 'error' ? 'danger' : flash.type %> alert-dismissible fade show mb-3">
          <%= flash.message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>
